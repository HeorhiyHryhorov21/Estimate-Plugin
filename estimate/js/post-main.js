jQuery(document).ready(function() {

	/*jQuery(document).on('click', 'a#print_link', function(){
			estimate_title = jQuery(this).closest('td').find('a.row-title').html();
			estimate_print = {
					action: 'print_estimate',
					estimate_post_title: estimate_post_title
				}
		    jQuery.post(post_js_object.ajax_url, estimate_print, function(data) {
		  
		    });
     });*/
});
